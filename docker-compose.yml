version: '3.7'

services:
  backend:
    build: ./EdgeUsher-backend
    network_mode: host
    ports: 
      - "5000:5000"
  frontend:
    build: ./EdgeUsher-frontend
    network_mode: host
    ports:
      - "4200:4200"
    depends_on:
      - backend
